import React from 'react';
import {ErrorpageTitle} from "./styled";

const Errorpage = () => {
    return (
        <ErrorpageTitle>
            Page not found.
        </ErrorpageTitle>
    );
};

export default Errorpage;
